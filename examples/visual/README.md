# Visual Examples

Visual Steam examples should use Node3D runtime packages such as
`@node-3d/core` when they need rendering, windowing, or an update loop tied to
visual output.

Do not add Electron or NW.js examples here.
